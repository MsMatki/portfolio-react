import React from 'react';
import classes from './Hover.css'

const Hover = (props) => {
    return(
        <section>
            <div className={classes.Hover}>

            </div>
        </section>
    );
}

export default Hover;